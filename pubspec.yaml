name: owari
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  cached_network_image: ^3.2.0
  http: ^0.13.0
  get: ^4.6.5
  fluttertoast: ^8.2.2
  shared_preferences: ^2.1.2
  simple_animations: ^5.0.1
  image_picker: ^0.8.4+3
  flutter_html: ^2.2.1
  path_provider: ^2.0.15
  url_launcher: ^6.1.11
  intl: ^0.18.1
  share_plus: ^6.3.4
  flutter_share: ^2.0.0
  # flutter_share: 2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  # flutter_native_splash: ^2.3.0

# flutter_native_splash:
#   image: assets/logo1.png

#   color: "#f9f9f9"

flutter:
  uses-material-design: true
  assets:
    - assets/
    # - assets/1.jpg
  # fonts:
  #   - family: MonumentExtended
  #     fonts:
  #       - asset: assets/font/MonumenExtended-Reguler.otf
